package idx2d;

import java.awt.*;
import java.io.*;

public abstract class ImageCodec
{
	public static Image read(File file)
	{
		return read(new FileInputStream(file));
	}
	
	public abstract static Image read(InputStream in);
}
